         # Bounds Check (ObsValue)
         - filter: Bounds Check
           filter variables:
           - name: stationPressure
           minvalue: 20000.0
           maxvalue: 120000.0
           action:
             name: reduce obs space

